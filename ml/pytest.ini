[pytest]
testpaths = tests
# Repo root on the path so tests import `ml.*` the same way the service does.
pythonpath = ..
markers =
    slow: end-to-end checks that train a small model (a few seconds each)
filterwarnings =
    ignore::DeprecationWarning
